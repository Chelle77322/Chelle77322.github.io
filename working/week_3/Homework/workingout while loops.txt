for (var i = 0; i < passwordLength;)
    {
      console.log(i);


      //randomly extracts characters from all arrays if every thing is true
      if (confirmLower === true && confirmUpper === true && confirmNumber === true && confirmSymbol === true) {
        var randNum = Math.floor(Math.random() * useLowers.length + useUppers.length + useNumbers.length + useSymbols.length);
        console.log(randNum);

        var random_password = useLowers[randNum]+ useUppers[randNum]+ useNumbers[randNum]+ useSymbols[randNum];
        console.log(random_password);
        passArrays.push(random_password);


        console.log(passArrays);
      }
    }
  }

for (var i = 0; i < passwordLength;)
    {
      console.log(i);

while (i < passwordLength){
if (confirmLower === true && confirmUpper === true && confirmNumber === true && confirmSymbol === true) {
        var randNum = Math.floor(Math.random() * useLowers.length + useUppers.length + useNumbers.length + useSymbols.length);
        console.log(randNum);

        var random_password = useLowers[randNum]+ useUppers[randNum]+ useNumbers[randNum]+ useSymbols[randNum];
        console.log(random_password);
        passArrays.push(random_password);


        console.log(passArrays);
      }

}
}
